# Data (keep structure, ignore large raw files if needed — adjust as you like)
data/raw/*.csv
data/processed/*.csv

# Python
__pycache__/
*.pyc
venv/
.env
.ipynb_checkpoints/

# MLflow
mlruns/
mlflow.db

# Models (large binary files — track via Git LFS if needed)
models/*.pkl
models/*.joblib

# OS / Editor
.DS_Store
.vscode/
.idea/
